.container(ng-if="ctrl.lesson")
  .panel.panel-primary
    .panel-heading.text-center {{ctrl.lesson.title}}
    .panel-body.text-center
      video-player(config="ctrl.config", video-current-time="ctrl.videoCurrentTime")
.container
  issues-list(lesson="ctrl.lesson", friendly="true", persistant="true")